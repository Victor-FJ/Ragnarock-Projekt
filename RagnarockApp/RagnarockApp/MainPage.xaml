<Page
    x:Class="RagnarockApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RagnarockApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" d:DesignWidth="961.101" d:DesignHeight="580.645">

    <Page.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Width" Value="{Binding CompactPaneLength, ElementName=MenuSplitView, Mode=OneTime}"/>
        </Style>
    </Page.Resources>

    <SplitView DisplayMode="CompactOverlay" IsPaneOpen="False" CompactPaneLength="50" OpenPaneLength="170">
        <SplitView.Pane>
            <StackPanel Background="Gray">
                <Button Style="{StaticResource MenuButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="&#xE77B;" FontSize="18" VerticalAlignment="Center" FontFamily="Segoe MDL2 Assets" Margin="6"/>
                        <TextBlock Text="User" FontSize="18" VerticalAlignment="Center" />
                    </StackPanel>
                    <interactivity:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Click">
                            <core:NavigateToPageAction TargetPage="RagnarockApp.UserNicolai.View.UserPage"/>
                        </core:EventTriggerBehavior>
                    </interactivity:Interaction.Behaviors>
                </Button>
                <Button Style="{StaticResource MenuButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="&#xE899;" FontSize="18" VerticalAlignment="Center" FontFamily="Segoe MDL2 Assets" Margin="6"/>
                        <TextBlock Text="Event" FontSize="18" VerticalAlignment="Center" />
                    </StackPanel>
                    <interactivity:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Click">
                            <core:NavigateToPageAction TargetPage="RagnarockApp.EventArsen.View.EventPage"/>
                        </core:EventTriggerBehavior>
                    </interactivity:Interaction.Behaviors>
                </Button>
                <Button Style="{StaticResource MenuButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="&#xE7FC;" FontSize="18" VerticalAlignment="Center" FontFamily="Segoe MDL2 Assets" Margin="6"/>
                        <TextBlock Text="Quiz" FontSize="18" VerticalAlignment="Center" />
                    </StackPanel>
                    <interactivity:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Click">
                            <core:NavigateToPageAction TargetPage="RagnarockApp.QuizVictor.View.EditQuizPage"/>
                        </core:EventTriggerBehavior>
                    </interactivity:Interaction.Behaviors>
                </Button>
            </StackPanel>
        </SplitView.Pane>
        <Grid>
            <Frame x:Name="MainFrame"/>
        </Grid>
    </SplitView>

</Page>
